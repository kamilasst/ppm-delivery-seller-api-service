package com.ppm.delivery.seller.api.service.constantsApi;

// TODO Review - Classe nao esta sendo usado, por favor apagar(classe e pacote). Mesmo que estivesse, está com o nome de mock
// deveria está no pacote de testes
public class ConstantsMocks {

    public static final String ISO_UTC_MILLISECONDS_PATTERN = "yyyy-MM-dd'T'HH:mm:ss.SSS'Z'";

    private ConstantsMocks() {
        throw new UnsupportedOperationException("Utility class");
    }
}
